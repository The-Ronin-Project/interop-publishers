query PractitionerList(
    $identifier: string,
    $count: integer,
    $page: integer
){
    PractitionerList(identifier: $identifier, _count: $count, _page: $page) {
        identifier{
            system
            type{
                text
            }
            value
        }
        id
    }
}
