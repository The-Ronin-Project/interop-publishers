query PractitionerList(
    $identifier: string,
    ){
    PractitionerList(identifier: $identifier) {
        identifier{
            system
            type{
                text
            }
            value
        },
        id
    }
}
