query($tenant:string!, $identifiers:string!) {
    PractitionerList(
        identifier: $tenant
        identifier_list: [$identifiers]
    )
    {
        id
        identifier {
            system
            value
        }
    }
}

