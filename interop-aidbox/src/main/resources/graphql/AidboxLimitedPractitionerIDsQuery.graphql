query Practitioner($tenant:string, $id:string) {
    PractitionerList(_id:$id, identifier: $tenant) {
        identifier{
            system
            type{
                text
            }
            value
        }
    }
}
